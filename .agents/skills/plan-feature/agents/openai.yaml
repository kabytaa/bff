interface:
  display_name: "Plan Feature"
  short_description: "Plan an accepted direction for later execution"
  default_prompt: "Use $plan-feature to turn the accepted direction into an implementation-ready plan, then wait for my review before execution."
