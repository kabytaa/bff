interface:
  display_name: "Cleanup Code"
  short_description: "Improve changed code without altering behavior"
  default_prompt: "Use $cleanup-code to clean up the recently changed code and validate it."
